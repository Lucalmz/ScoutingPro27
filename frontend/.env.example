# ---------------------------------------------------------------------------
# ScoutingPro27 前端构建期环境变量示例
# 复制为 .env / .env.local 后按需填写；.env* (除 .env.example) 已被 git 忽略。
# ---------------------------------------------------------------------------

# 现场网络诊断控制台开关（详见 src/config/features.ts）
# 正式上线建议设为 false，或运行 `npm run purge:diagnostics` 物理移除。
VITE_ENABLE_DIAGNOSTICS=true

# ---------------------------------------------------------------------------
# TURN 中继兜底（WebRTC ICE）
#
# 中继是「局域网直连 → STUN 穿透 → IPv6 直连」全部失败后的唯一逃生通道。
# 源码里的默认值是 Metered.ca 的公开示例凭据，属于**会过期/轮换的账号级令牌**：
# 一旦失效，iceTransportPolicy:"relay" 的重建将收集不到任何 relay 候选，
# 表现为“连不上、也降级不了”。生产部署请务必替换为自己申请的 TURN 服务。
#
# 建议同时确认该 TURN 服务具备 IPv6 监听地址（AAAA 记录），
# 否则纯 IPv6 网络下中继兜底同样不可用。
# ---------------------------------------------------------------------------
# VITE_TURN_HOST=turn.example.com
# VITE_TURN_USERNAME=your-turn-username
# VITE_TURN_CREDENTIAL=your-turn-credential
